[project]
name = "rag-arxiv-retrieval"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "arxiv-to-prompt>=0.11.1",
    "sentence-transformers>=5.3.0",
]
